import path from 'node:path'
import fs from 'fs'
import Cart from '../cart'

const p = path.join(__dirname, 'carts.json')

describe("first test in typescript", () =>{{
    test("", () =>{
        // Cart.addProduct(0, 10)
        // fs.readFile(p, 'utf-8', (err, fileContent) =>{
        //     if (err !== null) return console.error("does not have file")
        //     const parseFile = JSON.parse(fileContent);
        //     for (let item of parseFile){
        //         expect(item).toEqual(expect.objectContaining({
        //             id:0,
        //             qty:expect.any(Number)
        //         }))
        //     }
        // })
    })
}})

